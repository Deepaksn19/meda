# Runtime dependencies (same as pyproject.toml). Prefer `pip install -e ".[dev]"`.
numpy>=1.23
gymnasium>=0.29
stable-baselines3>=2.2
torch>=2.0
opencv-python-headless>=4.5
pyyaml>=6.0
matplotlib>=3.6
pandas>=1.5
pytest>=7
